# 虚拟长者全量测试工作台：界面增量简报

## 产品任务

在既有“方案 1：画像联调台”的左控右看结构内，让产品经理无需命令行即可选择一位带合成中文姓名的长者，按全部 22 项、单项或多项运行测试，并在右侧持续看到真实产品问答、工具调用和语音交互过程。历史记录必须区分通过、未通过、受阻和取消，并允许从原记录发起不可篡改的重测。

## 用户与环境

- 核心用户是产品经理、站点验收人员和测试人员；陪同用户是现场工作人员。
- macOS Electron 为当前可验证平台，主目标视口为 1440×1024；750×1200 提供单栏切换。双屏推荐为控制台与数字人产品面分屏展示，但单屏必须完整可用。
- 主要输入为鼠标和触控；语音测试由系统自动播报固定合成问题、实际播放、本地识别并把识别文本送入业务 Harness，不开放生产自由输入。

## 视觉与交互约束

- 继续使用冷白、医疗蓝、低饱和状态表面和中文系统字体；不新建品牌视觉，不替换生产数字人素材。
- 禁止任何卡片、指标、告警或选中态使用单侧彩色竖线；状态通过整体表面色、图标、文字和必要的完整容器边界表达。
- 保持左侧“选人—选轮次—启动/停止”、右侧“真实产品观察”的主线。22 项选择需显示所选数量、自动补齐的前置项和实际执行数量。
- 必测语音需分别显示合成、播放、识别、业务响应、回答合成和回答播放；缺模型、静音、暂停、超时、识别偏差或播放不完整均不可显示为通过。
- 失败与受阻记录必须有清楚原因和“重新测试”操作；重测创建新 run/session/report，保留 retryOf，不覆盖原报告。

## 状态覆盖

未选居民、搜索中、无结果、加载失败、未选轮次、准备中、逐轮运行、等待右侧确认、语音阶段运行、权限受阻、语音受阻、业务失败、请求停止、已取消、部分完成、完整完成、历史记录加载失败和重测准备失败。

## 质量与安全边界

- 正文至少 14px，辅助文字至少 12px；主要操作和选择行至少 52px；完整可见焦点，Escape 关闭对话框并恢复焦点。
- 右侧产品画布保持原比例，不拉伸人物；宽屏双栏，窄屏明确切换“选择画像/观察界面”，不强塞两列。
- 合成姓名只是显示标签，身份绑定仍由 residentId、datasetVersion、seed、runId 和 sessionId 决定。
- 测试数据只使用隔离 Fixture；生产 MCP、真实身份、Windows 实机、物理麦克风和扬声器声学效果分别标记未验证。

## 成功与失败定义

成功意味着产品经理能从正常 App 入口完成选人、选择轮次、启动、观察、停止、查看记录和重测；所有被执行轮次均有文本、工具、语音阶段和实际播放器凭证，且失败统计可用于后续优化。任何文字结果冒充语音通过、音频未播放却确认完成、重测覆盖原记录、身份串线、前置项隐式执行、状态只靠颜色或单侧描边、关键路径需要命令行，均定义为失败。
